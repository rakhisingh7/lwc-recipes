name: Validate Deployment
on: 
  workflow_dispatch:
  push: 
    branches:
      - main
      - dev
jobs:
  pmd-scan:
    runs-on: ubuntu-latest
    container:
      image: salesforce/cli:latest-rc-slim
    steps:
      - name: PMD scan
        id: result
        uses: ./github/workflows/pmd.yml
      - name: Print PMD scan status
        run: echo "PMD Scan status ${{ steps.result.outputs.pmd_scan_status }}"

